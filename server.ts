// Local-development entry for OpenCode 2. Point a config at this checkout,
// e.g. "plugins": ["/path/to/opencode-supermemory"], and OpenCode loads the
// TypeScript sources directly. The published package uses dist/server.js.
export { default } from "./src/server.ts";
